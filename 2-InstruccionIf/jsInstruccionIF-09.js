function mostrar()
{
	let nota;

	//numero = parseInt(Math.random([1,10]));
	//numero = Math.round(Math.random() * (maximo - minimo) + minimo );
	
	// MAS FACIL QUE LA MIERDA HACERLO ASÍ
	nota = Math.round(Math.random() * + 1);
	/*alert(numero);
	*/



	else if (nota == 9 || numero == 10) {
		alert("¡EXCELENTE! Su nota es " + nota);
	} if (nota >=4 || nota <= 8) {
		alert("APROBÓ, su nota es " + nota);
	} else {
		alert("Vamos, la proxima se puede.");
	}










	//Genero el número RANDOM entre 1 y 10 
	//alert(Math.random[1,10]);	

}//FIN DE LA FUNCIÓN